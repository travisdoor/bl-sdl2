// Build tools for SDL2 needed on Windows.
#load "sdl2/build.bl"

build :: fn () #build_entry {
    exe :: add_executable("sdt-test");
    add_unit(exe, "main.bl");

    // Copy sdl2 dlls to the current directory.
    sdl2_copy_deps();
}